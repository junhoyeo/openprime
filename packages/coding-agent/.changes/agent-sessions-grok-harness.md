- Extended the bundled `agent-sessions` skill to index Grok Build transcripts (`~/.grok/sessions/<url-encoded-cwd>/<uuid>/updates.jsonl`): the ACP `session/update` stream maps to the same normalized events as every other harness, tool wall time comes from the gap between a `tool_call` and its terminal `tool_call_update`, and `cwd`/`title`/`started` come from the sibling `summary.json` with the percent-encoded directory name as the fallback `cwd`.
