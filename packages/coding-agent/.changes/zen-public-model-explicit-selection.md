- Fixed explicit selection of a zero-cost OpenCode Zen model failing with "No API key found for opencode." when no OpenCode credential is stored. `--model opencode/union-alpha`, `/model opencode/union-alpha`, a `set_model` command and `rlm.spawn(..., model="opencode/union-alpha")` now commit and serve requests with OpenCode's public key.
- The new `ModelRegistry.isExplicitlySelectable()` predicate (`hasConfiguredAuth() || isOpencodePublicModel()`) gates only explicit selection. `hasConfiguredAuth()` and `getAvailable()` are unchanged, so free Zen models still stay out of `/model` cycling, the picker's configured set, subagent discovery (`rlm.find_models`), session restore and backup-model resolution for users who never configured OpenCode.
