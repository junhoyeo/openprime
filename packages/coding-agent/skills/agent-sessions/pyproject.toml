[project]
name = "prime-agent-skill-agent-sessions"
version = "0.1.0"
description = "Read Codex CLI, Claude Code, Kimi, Kiro CLI, Prime Agent, and Senpi session transcripts from disk."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
agent_sessions = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_sessions"]
